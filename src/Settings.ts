export class Settings {
  static gridX = 10;
  static gridY = 10;

  static setGridX(gridX: number) {
    Settings.gridX = gridX;
  }

  static setGridY(gridY: number) {
    Settings.gridY = gridY;
  }
}